# Rust
/target
**/*.rs.bk
# Cargo.lock EST commité (application binaire → builds reproductibles, sécurité supply chain)

# Node / site build
node_modules/
dist/
.vite/

# Secrets & état d'affaires — NE JAMAIS COMMIT
# (gitignore n'accepte PAS de commentaire en fin de ligne : le motif doit être seul)
.env
.env.*
*.creds
*.secret
# état persisté des affaires (peut contenir des identifiants de clé) :
cases/
**/cases/
secrets/

# Données locales MinIO
.minio-data/
minio-data/

# Divers
.DS_Store
*.log
